export function add(x, y) {
  return x + y;
}

export function subtract(x, y) {
  return x - y;
}

export function multiply(x, y) {
  return x * y;
}

export default {
  add: add,
  subtract: subtract,
  multiply: multiply,
};
